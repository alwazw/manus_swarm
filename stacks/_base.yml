

###########

# _base.yml

# Define a common service template for reuse
x-common-service: &default-app-config
  restart: unless-stopped
  environment:
    PUID: ${PUID}
    PGID: ${PGID}
    TZ: ${TZ}

